{
  "version": 2,
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/cadastro-pacientes-exames/browser",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
